package newangle.xagent.domain.agent;

import lombok.NoArgsConstructor;

/**
 * Esquema de segurança para Mutual TLS (mTLS).
 * A descrição (opcional) é herdada da classe pai.
 */
@NoArgsConstructor
public class MutualTLSSecurityScheme extends SecurityScheme {
    // Não possui campos adicionais além de 'type' e 'description'.
}